

工作任务

> 我们来讨论下catalog的模块，我们不需要有db概念， 只要有table 概念。一个table 引用了多个partition。所以catalog 有原数据 存储的概念。但是我们未来是分布式数据库。一个table肯定不是在一个机器上工作的。在机器上工作的最小单元是partition。甚至是segment 。我想了想最好是segment。 我们的segment是 范围概念所以我可以指定搜索 某个partition的某个范围。那么我在单机的视角上来看。我需要指定哪些partition的 id范围。也就是segment的范围需要进行查询。



* engine 提供一个crate table api。 传入table 。然后engine在自己目录下用tablename来做一个目录。目录内存一份meta信息。 json格式， meta包含 Schema 等我大概写一下伪代码

````
  create table {
    table_name: VARCHAR(50) PRIMARY KEY,  
    schema_info: string , \\json 格式
    parallel_workers : int , 
    work_idr: string
 ) PARTITION BY field_name;  

 当定义 PARTITION BY 后。我们通过数据的 字段。 为目录做数据分割
parallel_workers 代表并行可以写入的 partition个数。也就是partition数

目录结构是这样的

table_name
    ｜ schema 
    |（partition by filed）字段值 ,可以是多个|
        partiton-[num]
            segment-[start-end]
                | rowdata
                | deleted
                | pk_bloomfilter
                | fields
                    | [field_name]
                        |data
                        |node


`````




以下的内容你可以先不理会。
<!-- 我们利用gossip  未来做任务分发。比如我建立的并行度（parallel_workers）为 n。则找到n个机器（如果机器数小于n 则 有的机器会创建一个以上的partitionion ， 这里我们可以引入一个llm agent 来判断哪些机器合适。去通知它。
这些partition提供写入功能。（这个功能可以后面做）。前期我们随机指定机器。或者采用其他方式吧。 -->


