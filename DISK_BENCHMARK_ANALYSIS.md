# 磁盘版本性能基准测试分析

## 测试环境

- **日期**: 2025-01-XX
- **系统**: macOS (Release build)
- **数据集**: 10,000,000 条记录
- **存储对比**:
  - **Calm**: 100 个冻结的 segments,每个 100,000 条记录
    - 使用 inverted indexes (倒排索引)
    - RowData 基于 BTree 的磁盘存储
  - **DataFusion**: 单个 Parquet 文件
    - 文件大小: 173.51 MB
    - 列式存储,支持压缩和谓词下推

## 基准测试结果

| 查询 | 描述 | Calm (ms) | Native (ms) | 慢倍数 |
|------|------|-----------|-------------|--------|
| Q1 | COUNT(*) 全表扫描 | 205.95 | 0.39 | **523.66x** |
| Q2 | category = 'A' (20%数据) | 53.92 | 11.15 | **4.84x** |
| Q3 | value >= 500 (50%数据) | 199.66 | 6.29 | **31.76x** |
| Q4 | category='B' AND value<300 | 97.23 | 17.02 | **5.71x** |
| Q5 | GROUP BY + AVG聚合 | 6073.63 | 37.71 | **161.07x** |
| Q6 | timestamp BETWEEN (0.01%数据) | 3049.49 | 2.26 | **1349.44x** |
| Q7 | category='A' OR 'C' (40%数据) | 110.83 | 19.04 | **5.82x** |

## 详细分析

### 1. 全表扫描 (Q1: COUNT(*))

**Calm**: 205.95ms | **DataFusion**: 0.39ms | **慢 523.66x**

#### 为什么 Calm 这么慢?

1. **Union 开销**: 需要合并 100 个 segments 的结果
2. **逐行处理**: 虽然是 COUNT(*),但仍需遍历所有 doc_ids
3. **内存分配**: 每个 segment 创建 RecordBatch

#### DataFusion 为什么这么快?

1. **Parquet 元数据**: 直接从文件元数据读取行数,无需扫描数据!
2. **零数据读取**: COUNT(*) 不需要读取任何列数据
3. **单一文件**: 无需合并多个数据源

**优化方向**:

- Calm 应该在 Segment 元数据中存储 doc_count
- COUNT(*) 查询应该短路,直接返回 sum(segment.doc_count())

### 2. 选择性查询 (Q2: category = 'A')

**Calm**: 53.92ms | **DataFusion**: 11.15ms | **慢 4.84x**

这是 Calm 相对表现最好的场景!

#### Calm 的优势

1. **倒排索引**: 快速定位 category='A' 的所有 doc_ids
2. **避免全扫描**: 只读取 20% 的数据

#### 为什么还是慢 4.84x?

1. **100 段 Union**: 需要查询 100 个倒排索引并合并结果
2. **行式提取**: 逐行从 RowDataStore 提取数据
3. **RecordBatch 构建**: 为每个 segment 构建小 batch,然后合并

#### DataFusion 的对抗策略

即使是全扫描,DataFusion 的列式+向量化执行也很快:

- **列式读取**: 只读 category 列进行过滤
- **向量化**: 一次处理 1024 行
- **SIMD**: 利用 CPU 向量指令

**结论**: 选择性 20% 的查询下,Calm 的索引优势能将差距缩小到 5x

### 3. 范围查询 (Q3: value >= 500)

**Calm**: 199.66ms | **DataFusion**: 6.29ms | **慢 31.76x**

#### 问题诊断

这个查询返回 50% 的数据,Calm 的表现接近全扫描:

1. **倒排索引不适合范围**: 需要查询 500 个不同的 value 值
2. **索引合并开销**: 合并 500 个 posting lists
3. **数据量大**: 返回 5,000,000 条记录

#### DataFusion 优势

1. **列式存储**: 只需读取 value 列进行过滤
2. **向量化比较**: 批量执行 `>= 500` 比较
3. **高效序列化**: Parquet 压缩降低 I/O

**优化思路**:

- 对数值字段使用 BTree 索引而不是倒排索引
- 支持范围查询的索引结构

### 4. 多条件过滤 (Q4: category='B' AND value<300)

**Calm**: 97.23ms | **DataFusion**: 17.02ms | **慢 5.71x**

选择性约 6% (20% * 30%),Calm 表现较好。

#### 索引交集

Calm 可以:

1. 从 category 索引获取 'B' 的 doc_ids (20%)
2. 从 value 索引获取 < 300 的 doc_ids (30%)
3. 取交集 (理论上 6%)

但实际上:

- 100 个 segments,每个需要做交集
- Union 开销仍然存在
- 逐行提取数据

### 5. 聚合查询 (Q5: GROUP BY + AVG)

**Calm**: 6073.63ms | **DataFusion**: 37.71ms | **慢 161.07x**

这是差距第二大的查询类型。

#### 为什么 Calm 在聚合上这么慢?

1. **缺乏列式处理**: 需要提取完整行,然后解析 value 字段
2. **内存复制**: 数据从 RowData → RecordBatch → 聚合算子
3. **无向量化**: 逐行累加计算 AVG

#### DataFusion 的聚合优势

1. **列式聚合**: 直接在 value 列上计算,无需其他字段
2. **向量化计算**: 使用 SIMD 批量求和
3. **内存布局优化**: 数据在内存中连续,缓存友好

**关键差距**: 列式 vs 行式架构的根本差异

### 6. 小范围查询 (Q6: BETWEEN 0~1000)

**Calm**: 3049.49ms | **DataFusion**: 2.26ms | **慢 1349.44x**

这是差距最大的查询! 只查询 0.01% 的数据 (1000 条记录)。

#### 为什么 Calm 这么慢?

这暴露了严重的性能问题:

1. **100 段扫描**: 即使只返回 1000 条,也要查询 100 个 segments
2. **缺少范围索引**: timestamp 字段可能没有高效的范围查询索引
3. **Union 开销**: 合并 100 个结果的开销远大于查询 1000 条数据本身

#### DataFusion 为什么这么快?

1. **Parquet 谓词下推**: 在读取 Row Group 时就过滤
2. **Page 级别跳过**: 使用 min/max 统计信息跳过整个 Page
3. **单文件扫描**: 无需合并多个数据源

**严重问题**: 对于高选择性查询,Calm 反而更慢!

这说明:

- 100 个 segments 的 Union 开销太大
- 需要更智能的 segment 剪枝策略
- timestamp 字段需要范围索引

### 7. 多值过滤 (Q7: OR 条件)

**Calm**: 110.83ms | **DataFusion**: 19.04ms | **慢 5.82x**

返回 40% 的数据,Calm 表现中等。

#### OR 查询处理

Calm:

1. 查询 category='A' 的 posting list
2. 查询 category='C' 的 posting list
3. 取并集 (union)
4. 提取对应的行数据

DataFusion:

- 全扫描 category 列,向量化判断 `== 'A' || == 'C'`

**结论**: 对于中等选择性的 OR 查询,索引优势有限

## 总体结论

### Calm 的核心瓶颈

1. **100 段 Union**:
   - 每个查询都需要执行 100 次,即使返回数据很少
   - Union 算子本身有显著开销
   - **优化**: Segment 剪枝,partition pruning

2. **行式存储架构**:
   - COUNT(*), AVG 等聚合查询需要提取完整行
   - 无法利用列式处理优势
   - **优化**: 考虑列式 RowData 存储

3. **缺乏向量化**:
   - 逐行处理数据
   - 无法利用 SIMD 等 CPU 优化
   - **优化**: 批量处理,使用 Arrow 的计算内核

4. **索引局限性**:
   - 倒排索引不适合范围查询
   - 缺少统计信息用于查询优化
   - **优化**: BTree 索引支持范围,元数据统计

### Calm 的潜在优势

1. **写入性能**:
   - 增量索引构建
   - 内存段缓冲

2. **实时查询**:
   - 可以查询未 flush 的内存数据
   - 无需等待 Parquet 文件生成

3. **更新/删除**:
   - 支持行级别的更新和删除
   - Parquet 是不可变的

4. **选择性查询**:
   - 当选择性很高时 (<5% 数据),索引有明显优势
   - 可以跳过大部分数据读取

### 磁盘 vs 内存对比

对比之前的内存版本测试,磁盘版本的性能几乎相同:

- **Calm**: 差异 <5%
- **DataFusion**: 差异 <10%

**说明**:

- 两个系统的 I/O 都很高效
- 性能差距主要来自**查询处理架构**,而非存储层
- Parquet 的压缩和缓存机制很高效

## 优化建议 (按优先级)

### 高优先级

1. **元数据优化 COUNT(*)**

   ```rust
   // 直接从元数据返回,无需扫描
   if query.is_count_star() {
       return segments.iter().map(|s| s.doc_count()).sum();
   }
   ```

   **预期提升**: COUNT(*) 从 206ms → <1ms

2. **Segment 剪枝**

   ```rust
   // 根据查询条件跳过整个 segments
   if segment.max_timestamp < filter.min_timestamp {
       continue; // 跳过这个 segment
   }
   ```

   **预期提升**: Q6 从 3049ms → <100ms

3. **批量数据提取**

   ```rust
   // 一次性提取多行,减少函数调用开销
   let rows = row_store.get_batch(&doc_ids[..]);
   ```

   **预期提升**: 所有查询 20-30%

### 中优先级

4. **向量化过滤**
   - 使用 Arrow 的 compute 内核
   - 批量比较,减少分支预测失败
   - **预期提升**: 过滤密集型查询 2-3x

5. **Segment 合并策略**
   - 减少 segment 数量 (100 → 10)
   - 类似 LSM-Tree 的 compaction
   - **预期提升**: Union 开销减少 90%

6. **范围索引**
   - 数值字段使用 BTree 而非倒排索引
   - 支持高效的范围查询
   - **预期提升**: Q3,Q6 等范围查询 5-10x

### 低优先级

7. **列式存储**
   - RowData 支持列式布局选项
   - 聚合查询只读取需要的列
   - **预期提升**: 聚合查询 5-10x

8. **查询下推**
   - 将聚合下推到 Segment 级别
   - 减少数据传输
   - **预期提升**: GROUP BY 查询 2-3x

9. **自适应执行**
   - 根据数据分布选择索引扫描 vs 全扫描
   - 动态调整执行策略
   - **预期提升**: 复杂查询 1.5-2x

## 预期优化效果

如果实现上述优化,预期性能:

| 查询 | 当前 (ms) | 优化后 (ms) | 与 DataFusion 比较 |
|------|-----------|-------------|-------------------|
| Q1 | 205.95 | **<1** | 接近或超过 |
| Q2 | 53.92 | **15-20** | 接近 |
| Q3 | 199.66 | **30-40** | 仍慢 5-6x |
| Q4 | 97.23 | **20-30** | 接近 |
| Q5 | 6073.63 | **500-1000** | 仍慢 10-20x |
| Q6 | 3049.49 | **5-10** | 接近或超过 |
| Q7 | 110.83 | **30-40** | 接近 |

**结论**: 通过优化,Calm 可以在大多数场景下达到 DataFusion 的 80-90% 性能,同时保持实时写入和更新的优势。
